[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "bupt-data-agent"
version = "0.1.0"
description = "Business-aware Text-to-SQL data analysis agent"
readme = "README.md"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
